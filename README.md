# string-to-integer-problem-solve-code-in-hackkerrank
A Python solution for converting a string to an integer using exception handling. Prints the integer if conversion succeeds, otherwise prints "Bad String". Demonstrates use of try-except to handle invalid inputs without loops or conditional statements.
